{
    "builds":[{
        "src": "ovs1/wsgi.py",
        "use":"@vercel/python",
        "config":{ "maxLambdasize": "15mb", "runtime": "Python 3.10.6"}
    }],
    "routes":[
        {
            "src": "/(.*)",
            "dest": "ovs1/wsgi.py"
        }
    ]
}